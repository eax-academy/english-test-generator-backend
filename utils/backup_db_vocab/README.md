# Database Migration: SQLite → MongoDB

This project contains a SQLite backup (`data/vocab.sqlite`) with vocabulary entries.

## How to migrate

1. Install dependencies:
2. Run migration!!(only migrate.js)!!:
3. MongoDB should be running locally (`mongodb://localhost:27017/dbname`)
